# Week 02 学习复盘

请尽量先用自己的语言回答，再回看代码和 README。这里不要求一次全部答对，重点是确认自己能否解释提示词、结构化输出、自动修复、重试和评测之间的关系。

## 1. 本周交付结果

1. Week 02 实现了什么程序？输入和输出分别是什么？
2. `pnpm start`、`pnpm evaluate`、`pnpm test`、`pnpm typecheck` 和 `pnpm build` 分别有什么作用？
3. 本周实现了哪些可靠性能力？
4. 哪些功能可以通过自动化测试验证？哪些结果仍需要人工判断？

我的结论：

> 待补充

## 2. 提示词的组成与版本

1. 一份完整的提示词为什么需要区分指令、上下文、输入数据和输出约束？
2. `system` 消息和 `user` 消息在文章分析任务中分别承担什么职责？
3. zero-shot 和 few-shot 的区别是什么？示例可能给结果带来哪些好处和成本？
4. 为什么提示词版本需要使用稳定名称管理，而不是直接覆盖旧提示词？
5. `buildAnalysisMessages()` 为什么只负责构造消息，而不直接调用模型？

我的结论：

> 待补充

## 3. TypeScript、JSON 与 Zod Schema

1. 为什么 TypeScript 的 `ArticleAnalysis` 类型不能证明模型运行时返回的数据正确？
2. 模型返回文本后，需要经过哪些步骤才能成为可信的 `ArticleAnalysis`？
3. `invalid-json` 和 `schema-mismatch` 有什么区别？
4. 为什么不能直接使用 `as ArticleAnalysis` 跳过运行时校验？
5. Schema 中对摘要长度、关键点数量和关键词数量的约束有什么作用？

我的结论：

> 待补充

## 4. 输出自动修复

1. 什么情况下会触发输出修复？什么情况下不会触发？
2. 修复提示词中包含哪些信息？为什么需要保留第一次的错误输出？
3. 输出修复是程序直接修改 JSON，还是再次请求模型生成？
4. 为什么修复后的结果仍然必须重新经过 JSON 和 Zod 校验？
5. 为什么要限制 `maxRepairAttempts`，而不能无限修复？
6. `attempts` 与 `repairAttempts` 分别表示什么？

我的结论：

> 待补充

## 5. API 错误分类

1. `rate-limit`、`quota-exhausted`、`http-error`、`timeout`、`network-error` 和 `invalid-response` 分别发生在哪个层次？
2. 为什么 HTTP 429 既可能表示临时限流，也可能表示额度耗尽？
3. 为什么临时限流可以等待重试，而额度耗尽不应该重试？
4. 为什么 API 请求失败不能交给输出修复流程处理？
5. 为什么不应该把上游返回的完整错误正文直接写入报告或返回给用户？

我的结论：

> 待补充

## 6. 请求节流、退避重试与冷却

1. 请求节流解决什么问题？它和失败后的重试有什么区别？
2. 为什么首次分析、429 重试和输出修复请求必须共享同一个请求调度器？
3. `Retry-After`、指数退避和随机抖动分别有什么作用？
4. 为什么短重试耗尽后还要设置一次较长冷却？
5. 为什么重试次数、最大等待时间和冷却次数都必须有上限？
6. 当前机制能否保证永远不再出现 429？为什么？

我的结论：

> 待补充

## 7. 评测集与提示词对比

1. 为什么评测集需要同时包含正常、边界和对抗案例？
2. 为什么两个提示词版本必须运行相同案例，才具有可比性？
3. JSON 解析率、Schema 通过率、约束通过率和必需词覆盖率分别检查什么？
4. 为什么 429 等请求失败不应该被统计成提示词质量失败？
5. token、HTTP 尝试次数和耗时如何帮助判断方案成本？
6. 为什么自动指标不能完全替代人工评分？

我的结论：

> 待补充

## 8. 对抗输入与安全边界

1. 对抗文章中的“忽略之前指令”等文本为什么可能影响模型？
2. 提示词要求模型把文章当作数据，是否能彻底阻止提示词注入？
3. 为什么工具权限、参数限制和真实副作用不能只依赖 system prompt？
4. 当前文章分析程序没有工具调用，提示词注入的主要风险是什么？
5. 进入 Week 03 后，工具白名单和 Zod 参数校验为什么会更重要？

我的结论：

> 待补充

## 9. 确定性代码与模型职责

1. 提示词构造、文章总结、JSON 解析、Schema 校验、错误分类和重试决策中，哪些由模型负责，哪些由代码负责？
2. 为什么模型适合处理文章语义，却不适合决定自己的输出是否合格？
3. 为什么“让模型返回 JSON”和“代码确认它是合法业务 JSON”是两件不同的事？
4. 如果第二次修复依然失败，为什么程序应返回错误，而不是伪造一个默认分析结果？

我的结论：

> 待补充

## 10. 测试与依赖注入

1. 为什么测试中注入假的 `modelCaller` 和 `fetch`？
2. 如何模拟合法输出、非法 JSON、Schema 错误、临时 429 和额度耗尽？
3. 请求调度器为什么需要注入 `sleep` 和 `now` 才容易测试？
4. 单元测试全部通过，是否代表真实模型调用一定成功？为什么？
5. 修改核心流程后，为什么需要同时运行 typecheck、test 和 build？

我的结论：

> 待补充

## 11. 当前限制与改进方向

1. 请至少列出当前文章分析和评测方案的三个限制。
2. 当前词项覆盖率为什么只能作为粗略指标？
3. 当前评测为什么可能受到模型随机性、网络状态和账户额度影响？
4. 当前报告重新生成时会覆盖人工评分，这个问题可以如何改进？
5. 如果评测执行到一半中断，可以怎样增加检查点和恢复能力？

我的结论：

> 待补充

## 12. Week 03 衔接思考

1. 普通函数和 Agent 工具有什么关系？
2. 一个工具定义至少需要包含名称、用途、参数 Schema 和什么执行逻辑？
3. 为什么模型只能提出工具调用请求，而实际是否执行应由代码决定？
4. “模型决策—执行工具—返回观察结果—继续决策”为什么需要循环？
5. 最大步骤数、超时、重复调用保护和幂等性分别要解决什么问题？

我的结论：

> 待补充

## 13. 最终自检

完成回答后，请按真实掌握程度勾选：

- [ ] 我能解释 zero-shot、few-shot 和提示词版本管理。
- [ ] 我能说明 TypeScript 类型与 Zod 运行时校验的边界。
- [ ] 我能区分非法 JSON、Schema 错误和 API 请求错误。
- [ ] 我能完整描述输出自动修复的数据流。
- [ ] 我能区分请求节流、429 重试、长冷却和额度耗尽。
- [ ] 我能解释为什么所有真实 HTTP 尝试需要共享调度器。
- [ ] 我能说明自动评测与人工评分各自适合判断什么。
- [ ] 我能指出当前方案至少三个限制及改进方向。
- [ ] 我已经准备好进入 Week 03 的工具调用与 Agent 循环学习。

## 14. 本周总体结论

本周最重要的收获：

> 待补充

本周最难的知识点：

> 待补充

目前仍不确定的问题：

> 待补充

进入 Week 03 前准备采取的行动：

> 待补充
