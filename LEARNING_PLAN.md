# 前端开发者的 AI Agent 8 周学习计划

## 1. 学习目标

完成本计划后，应能够：

- 解释大语言模型、提示词、结构化输出和上下文窗口的基本原理；
- 使用 TypeScript 和模型 API 构建多轮对话与流式输出程序；
- 为 Agent 设计并实现工具调用、记忆、检索和任务规划；
- 处理超时、重试、循环调用、错误参数和提示词注入等问题；
- 使用测试集、轨迹日志、成本和延迟指标评估 Agent；
- 独立完成并展示一个可运行的 AI Agent 项目。

## 2. 学习安排

- 周期：8 周
- 投入：每周 6–8 小时
- 建议节奏：2 小时理论学习 + 4 小时编码 + 1 小时复盘
- 推荐语言：TypeScript；运行时使用 Node.js
- 推荐基础工具：pnpm、Vitest、Zod；界面框架沿用自己熟悉的 React/Vue
- 学习方法：每周至少完成一个可运行程序、一次测试和一篇复盘

开始前先按照[开发环境配置](./ENVIRONMENT.md)安装并检查工具。本计划统一使用 Node.js 24.x LTS 和 pnpm 11.x，减少不同版本造成的运行差异。

如果时间有限，可将每一周拆成两周；不要跳过第 7 周的评测环节。

## 3. 前端基础如何迁移

已有前端经验并不需要从零开始，以下能力可以直接迁移：

| 前端经验 | Agent 中的对应概念 |
| --- | --- |
| TypeScript 类型、表单校验 | 工具参数、结构化输出与运行时校验 |
| React/Vue 状态管理 | Agent 状态、对话历史与执行步骤 |
| `fetch`、REST、SSE | 模型 API、工具调用与流式输出 |
| 路由和中间件 | Agent 工作流、权限检查与人工确认 |
| 单元测试、E2E 测试 | 提示词回归测试与 Agent 轨迹评测 |
| 性能监控 | token、延迟、成本和工具成功率监控 |

需要重点补齐的内容：

- Node.js 服务端基础：环境变量、文件系统、数据库、并发与超时；
- 密钥和权限边界：模型 Key 只能保存在服务端，不能打包到浏览器；
- 大模型的不确定性：相同输入可能产生不同输出，不能只写固定断言；
- 数据与检索：文档切分、嵌入、向量检索和结果评测；
- Agent 工程：循环控制、持久化、重试、幂等和可观测性。

学习时采用“服务端能力优先、界面够用即可”的原则：每周先让核心逻辑在测试或命令行中跑通，再增加简单页面。建议将时间按核心 Agent 70%、服务端基础 20%、界面 10% 分配，避免把大部分时间花在样式和组件封装上。

## 4. 分周计划

### 第 1 周：大模型与 API 基础

学习内容：

- Transformer、大语言模型和 token 的基本概念（理解用途即可，不要求推导数学细节）；
- system、user、assistant 消息的职责；
- temperature、最大输出长度和上下文窗口；
- JSON 结构化输出、SSE/流式输出和常见 API 错误；
- Node.js 环境变量、异步流程和服务端 API 路由。

实践任务：

- [ ] 使用 TypeScript 编写一个命令行多轮聊天程序；
- [ ] 使用服务端环境变量读取 API Key，确保密钥不进入仓库和前端产物；
- [ ] 保存每轮输入、输出、耗时和 token 用量；
- [ ] 用 Zod 校验模型返回的 JSON，并编写解析失败测试；
- [ ] 可选：增加一个只有输入框和消息列表的简单页面。

交付物：`week01-chat/` 和一篇学习笔记。

### 第 2 周：提示词工程与可靠输出

学习内容：

- 指令、上下文、示例和输出约束的组织方法；
- zero-shot、few-shot 和任务分解；
- TypeScript 类型与运行时结构化输出校验、重试和降级策略；
- 提示词版本管理与回归测试。

实践任务：

- [ ] 实现一个“文章摘要 + 关键信息提取”程序；
- [ ] 使用 TypeScript 和 Zod 为输出定义明确的数据结构；
- [ ] 准备至少 10 个正常、边界和对抗样例；
- [ ] 比较两个提示词版本的正确率、耗时和成本。

交付物：`week02-prompting/`、测试集和对比结果。

### 第 3 周：工具调用与 Agent 循环

学习内容：

- 工具定义、参数校验和工具返回值设计，可类比前端的函数调用与表单校验；
- “思考—行动—观察”式 Agent 循环；
- 最大步数、超时、幂等性和错误恢复；
- 确定性代码与模型决策的职责边界。

实践任务：

- [ ] 实现计算器、时间查询和本地知识查询工具，至少一个工具使用 `fetch`；
- [ ] 让模型根据问题选择工具并整合结果；
- [ ] 限制单次任务的最大调用次数；
- [ ] 使用 `AbortController` 处理超时；
- [ ] 测试非法参数、网络异常和重复调用。

交付物：`week03-tool-agent/` 和 Agent 执行轨迹示例。

### 第 4 周：检索增强生成（RAG）

学习内容：

- 文档加载、切分、嵌入、向量检索和重排；
- 召回率、上下文相关性和答案忠实度；
- 引用来源、无答案检测和知识更新；
- RAG 与微调的适用边界。

实践任务：

- [ ] 为本仓库中的学习笔记建立知识库；
- [ ] 实现带来源引用的问答；
- [ ] 比较两种切分策略和不同召回数量；
- [ ] 对知识库没有答案的问题明确拒答；
- [ ] 用简单页面展示答案引用的文档片段和相关度。

交付物：`week04-rag/`、检索实验记录和最小评测集。

### 第 5 周：记忆与上下文管理

学习内容：

- 短期记忆、长期记忆和用户画像，并与前端状态、服务端状态区分；
- 对话摘要、滑动窗口和语义检索；
- 记忆写入、更新、冲突和遗忘策略；
- 隐私数据最小化与用户可控删除。

实践任务：

- [ ] 为聊天 Agent 增加跨会话偏好记忆，使用服务端文件或轻量数据库持久化；
- [ ] 设计“是否值得记住”的判断规则；
- [ ] 允许查看、修正和删除记忆；
- [ ] 验证错误记忆不会被静默传播。

交付物：`week05-memory/` 和记忆策略说明。

### 第 6 周：规划、工作流与多 Agent

学习内容：

- 固定工作流、动态规划和状态机；
- 任务拆分、依赖关系、检查点和人工确认；
- 单 Agent 与多 Agent 的复杂度权衡；
- 失败回滚、可恢复执行和状态持久化。

实践任务：

- [ ] 使用 TypeScript 判别联合或状态机实现“研究—整理—审校—输出”工作流；
- [ ] 记录每个步骤的输入、输出和状态；
- [ ] 支持从失败步骤继续执行；
- [ ] 在页面上可视化当前步骤、工具调用和失败状态；
- [ ] 比较单 Agent 和多角色方案的质量、成本与延迟。

交付物：`week06-workflow/` 和架构决策记录。

### 第 7 周：评测、可观测性与安全

学习内容：

- 离线评测、在线监控和人工评审；
- 任务成功率、工具正确率、延迟、token 与费用；
- 执行轨迹、日志、追踪和错误分类；
- 提示词注入、越权工具、敏感信息泄露和高风险操作确认；
- 浏览器与服务端的信任边界、SSRF 和密钥泄露风险。

实践任务：

- [ ] 使用 Vitest 建立至少 30 条分层测试用例；
- [ ] 自动生成评测报告，并保留人工抽查项；
- [ ] 对工具采用白名单、参数校验和最小权限；
- [ ] 加入提示词注入测试、调用预算和中止条件；
- [ ] 可选：使用 Playwright 验证聊天和人工确认流程。

交付物：`week07-evaluation/`、评测报告和风险清单。

### 第 8 周：综合项目

从以下方向选择一个：

- 学习助教 Web App：读取资料，制定学习任务，出题并反馈；
- 代码仓库助手：理解仓库，回答问题，在网页展示引用和修改建议；
- 个人研究助手：拆解主题，检索资料，流式生成带引用的报告；
- 客服 Agent：基于知识库答疑，识别风险并在界面中转人工。

最低功能要求：

- [ ] 至少调用 2 个工具；
- [ ] 提供一个可用的 Web 界面和服务端 API；
- [ ] 包含检索或长期记忆中的至少一种；
- [ ] 有最大步数、超时、重试和人工确认机制；
- [ ] 输出可追踪的执行日志；
- [ ] 有不少于 30 条测试用例及评测结果；
- [ ] README 包含架构、运行方法、限制和安全说明。

交付物：`capstone/`、演示材料和项目复盘。

## 5. 每周复盘模板

每周在对应目录中创建 `RETROSPECTIVE.md`，回答以下问题：

1. 本周完成了什么？哪些结果可以运行或复现？
2. 最难的问题是什么？最终如何定位和解决？
3. 哪些决策交给模型，哪些由确定性代码完成？为什么？
4. 当前方案在质量、延迟、成本和安全上有什么问题？
5. 下周要保留、停止和尝试什么？

## 6. 项目目录建议

```text
ai-agent-study/
├── README.md
├── LEARNING_PLAN.md
├── week01-chat/
├── week02-prompting/
├── week03-tool-agent/
├── week04-rag/
├── week05-memory/
├── week06-workflow/
├── week07-evaluation/
└── capstone/
```

目录按学习进度逐步创建，避免提前生成空目录。每个实践目录至少包含：

- `README.md`：目标、运行方法和设计说明；
- `src/`：源代码；
- `tests/`：自动化测试；
- `package.json`：脚本和依赖；
- `.env.example`：只保存变量名，不保存真实密钥；
- `RETROSPECTIVE.md`：学习复盘。

## 7. 完成标准

满足以下条件即可认为本轮学习完成：

- 8 周任务完成率不低于 80%；
- 综合项目能够由新用户根据 README 独立运行；
- 核心测试可重复执行，且结果有记录；
- 能用 10 分钟讲清 Agent 的状态、工具、记忆、评测与安全设计；
- 能指出至少 3 个已知限制，并给出下一步改进方案。

## 8. 今日即可开始

1. 安装 Node.js、pnpm、Git，并准备一个可调用的模型 API；
2. 创建 `week01-chat/README.md`，记录技术选择和运行目标；
3. 用 TypeScript 完成最小单轮问答，再逐步加入多轮历史、流式输出和日志；
4. 提交第一个可运行版本，并记录遇到的问题。
