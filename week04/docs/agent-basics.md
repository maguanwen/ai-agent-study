# Agent 学习笔记

Agent 将模型决策、工具执行和结果反馈组织成循环。模型提出调用请求，程序检查并执行，不是模型直接执行本地函数。

## 工具与参数

工具包含名称、描述、参数 Schema 和执行逻辑。模型生成的参数不可信，需要经过 JSON 解析与运行时校验。

## 错误恢复

工具失败后，程序可以将错误作为 tool 消息返回。模型根据反馈修正参数或说明失败，不保证每次修复成功。

## 重复检测与幂等

重复调用检测限制同一任务内相同请求的次数，避免无意义循环。业务幂等则避免同一个业务操作重复产生副作用，通常需要业务幂等键和持久化去重。
